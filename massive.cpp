#include <iostream>

using namespace std;

int main(int argc, const char * argv[]) {
    
    int arr[5];
    
    cout << "Enter five numbers: " << endl;
    
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    
    cin.get();
    return 0;
}
